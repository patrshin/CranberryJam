using UnityEngine;
using UnityEngine.UI;
using System.Collections;

public class AbilityUI : MonoBehaviour {
	SpriteRenderer spr; 
	public GameObject blimp; 
	
	Image cooldown;
	
	void Start () {
		spr = GetComponent<SpriteRenderer> ();
	}
	
	// Update is called once per frame
	void FixedUpdate () {
		//if (blimp.GetComponent<Blimp>.abi
	}
}
